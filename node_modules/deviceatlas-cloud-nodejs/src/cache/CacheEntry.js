"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CacheEntry = void 0;
var CacheEntry = /** @class */ (function () {
    function CacheEntry(data, expiry) {
        this.data = data;
        this.expiry = expiry;
    }
    CacheEntry.getNewEntry = function (data, expirySeconds) {
        var expiry = Date.now() + (expirySeconds * 1000);
        return new CacheEntry(data, expiry);
    };
    CacheEntry.prototype.isExpired = function () {
        return this.expiry < Date.now();
    };
    return CacheEntry;
}());
exports.CacheEntry = CacheEntry;
